import React from "react";

const LoadingScreen = () => {
  return (
    <div className="fixed z-[100] font-bold w-full h-full top-0 left-0 bg-white flex items-center justify-center">
      Loading Screen
    </div>
  );
};

export default LoadingScreen;
